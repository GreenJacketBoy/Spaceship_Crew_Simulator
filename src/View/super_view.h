#ifndef SUPER_VIEW_H
#define SUPER_VIEW_H

#include <stdio.h>
#include "colors.h"
#include "stdlib.h"

int promptForCommand(char* cmdPlusArgs, size_t maxLength);
size_t promptForSize_T(char *promptMessage);

#endif